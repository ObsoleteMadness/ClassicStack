// Package toml is the TOML config Codec adapter (koanf/go-toml) over
// core/config.Model (§4).
//
// Ring: ADAPTER (implements core/config.Codec; may import koanf/toml). Real
// impl lands in step D4.
package toml
