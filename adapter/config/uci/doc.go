// Package uci is the OpenWRT UCI config Codec adapter over core/config.Model:
// config 'classicstack', option/list, one section per component (§4).
//
// Ring: ADAPTER (implements core/config.Codec). Real impl lands in step D6.
package uci
