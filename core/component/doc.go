// Package component defines the one lifecycle contract every port, service,
// router, and transport satisfies, plus the optional capability interfaces the
// supervisor and UI discover by type assertion (§3).
//
// Ring: CORE (stdlib only). Real types land in step B1.
package component
