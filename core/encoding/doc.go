// Package encoding holds pure, reflection-free character-set tables
// (MacRoman<->UTF-8) that the default FilenameCodec adapter reuses (§10a-bis).
//
// Ring: CORE (stdlib only). Real tables land in step B8.
package encoding
